alias update="sudo apt update -y && sudo apt upgrade -y && sudo apt autoclean -y && sudo apt autoremove -y"
alias tmux="tmux -u"
alias tm="cd GayatriApp/gayatri_invoice/gayatriapp && tmux"

alias runserver="uv run manage.py runserver"
alias makemigrate="uv run manage.py makemigrate"
alias migrate="uv run manage.py migrate"
alias shell="uv run manage.py shell"

alias status="git status"
alias branch="git branch -av"

